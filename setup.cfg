[pytest]
norecursedirs = env
addopts = --doctest-modules -v -s

# pytest coverage options
[run]
omit =
    */tests/*
    */test_*
    */_compat.py

[bdist_wheel]
universal = 1

[pep8]
exclude = .tox,env
